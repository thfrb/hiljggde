apiVersion: v1
kind: Service
metadata:
  name: foot
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "foot"
    port: 8080
  selector:
    app: foot
