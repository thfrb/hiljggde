apiVersion: v1
kind: Service
metadata:
  name: daar
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "daar"
    port: 8080
  selector:
    app: daar
