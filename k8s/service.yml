apiVersion: v1
kind: Service
metadata:
  name: bite
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bite"
    port: 8080
  selector:
    app: bite
