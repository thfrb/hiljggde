apiVersion: v1
kind: Service
metadata:
  name: line
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "line"
    port: 8080
  selector:
    app: line
