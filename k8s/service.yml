apiVersion: v1
kind: Service
metadata:
  name: live
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "live"
    port: 8080
  selector:
    app: live
