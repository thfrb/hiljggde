apiVersion: v1
kind: Service
metadata:
  name: kill
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "kill"
    port: 8080
  selector:
    app: kill
