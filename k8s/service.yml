apiVersion: v1
kind: Service
metadata:
  name: three
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "three"
    port: 8080
  selector:
    app: three
