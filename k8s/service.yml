apiVersion: v1
kind: Service
metadata:
  name: nana
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "nana"
    port: 8080
  selector:
    app: nana
