apiVersion: v1
kind: Service
metadata:
  name: emma
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "emma"
    port: 8080
  selector:
    app: emma
